[package]
name = "Agent"
version = "0.1.0"
edition = "2021"

[dependencies]
winapi = { version = "0.3", features = [
    "winbase",
    "winnt",
    "handleapi",
    "errhandlingapi",
    "fileapi",
    "synchapi",
    "minwindef",
    "winerror",
] }